#include <iostream>
using namespace std;
int main() {
	int n, k=1, s=1;
	cin >> n;
	if (n <= 1)
		cout << "Данные введены некорректно";
	else {
		while (s < n) {
			k++;
		s = ((1 + k) * k) / 2;
		}
		cout << k << " " << s;
	}
}
